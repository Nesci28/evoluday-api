components:
  parameters:
    BlankId:
      name: id
      description: Blank's Id
      in: path
      required: true
      schema:
        $ref: "../../../../node_modules/@yest/contract/resources.yaml#/components/schemas/ObjectID"
    IsBlankDryRun:
        name: isDryRun
        description: Should this request be ran as a dry run (no save)
        in: query
        required: false
        schema:
          type: boolean
    IsBlankArchived:
      name: isArchived
      description: Get archived Blank
      in: query
      required: false
      schema:
        type: boolean
