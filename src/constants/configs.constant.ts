import * as appRoot from "app-root-path";

export const configs = {
  appRoot,
  resolvedPath: `${appRoot}/src/definitions/resolved-deep.yaml`,
  emailTemplatesPath: `${appRoot}/src/mails`,
  forgotPasswordURLfr: "https://evoluday.com/reinitialisation",
  forgotPasswordURLen: "https://evoluday.com/reset",
};
